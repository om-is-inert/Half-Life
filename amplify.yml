version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd dashboard
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dashboard/dist
    files:
      - "**/*"
  cache:
    paths:
      - dashboard/node_modules/**/*
